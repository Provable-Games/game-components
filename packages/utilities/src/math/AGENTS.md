## Package: math

Fixed-point math library using 32.32 bit representation. Based on Cubit by Influence.

Provides `Fixed` type, `FixedTrait`, and mathematical functions (exp, ln, log2, pow, sqrt).

## Dependencies
- `starknet` only (leaf package)
